DELETE
FROM STUDENT
WHERE GROUPNUMBER >= 4;

DELETE
FROM STUDENT
WHERE STUDENT.ID IN (
    SELECT STUDENT_ID
    FROM MARK
    WHERE MARK.MARK < 4
    );

DELETE FROM PAYMENT
WHERE TYPE_ID = 1;

DELETE FROM MARK
WHERE MARK < 7;